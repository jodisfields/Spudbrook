# Docker Install Script
    ```
    git clone https://github.com/jodisfields/Spudbrook.git
    cd Spudbrook
    sh setup.sh
    ```
# Packages Installed
    apt-transport-https
    ca-certificates
    curl
    gnupg
    lsb-release
    docker
    docker-compose
# Deployments
    Wiki.JS
